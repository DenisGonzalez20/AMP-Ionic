import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prestaditoIT.AMP',
  appName: 'AMP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
